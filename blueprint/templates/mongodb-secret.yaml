{{- if .Values.mongodb_operator.enabled }}
{{- $password := .Values.mongodb_operator.password | default (randAlphaNum 16) }}
{{- range $i, $v := .Values.mongodb_operator.clusters }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $v.clusterName | quote }}
type: Opaque
stringData:
  password: {{ $password | quote }}
  username: {{ $v.username | quote }}
---
{{- end }}
{{- end }}