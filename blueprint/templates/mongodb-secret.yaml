{{- if .Values.backend.enabled  }}
{{- range $j, $d := .Values.backend.deployments }}
{{- if $d.mongodb.enabled }}
{{- $password := $d.mongodb.password | default (randAlphaNum 16) }}
{{- range $i, $v := $d.mongodb.instances }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $v.clusterName | quote }}
type: Opaque
stringData:
  password: {{ $password | quote }}
  username: {{ $v.username | quote }}
---
{{- end }}
{{- end }}
{{- end }}
{{- end }}